# All values intentionally empty. Run npm run setup for local-only credentials.
# No AI provider, database or webhook credentials are accepted by this demo.
SESSION_SECRET=
ADMIN_PASSWORD_HASH=
OPERATOR_PASSWORD_HASH=
REVIEWER_PASSWORD_HASH=
PORT=
PUBLIC_ORIGIN=
NODE_ENV=
TRUST_PROXY=
DATA_DIR=
